# CLAUDE.md

このリポジトリは、和歌山市のバドミントンサークル **遊部（ASOBU）** のInstagram投稿を
GitHub Actions 上で自動生成するためのもの。Claude はこのファイルの内容を前提に作業する。

---

## 1. サークルの基本情報（投稿に書いてよい事実）

| 項目 | 内容 |
|---|---|
| 名前 | 遊部（ASOBU） |
| エリア | 和歌山市 |
| 頻度 | 週1回。**2026年9月は毎週日曜（6・13・20・27）**。日程は本人が月ごとのカレンダー画像で共有する |
| 会場 | 和歌山市内の体育館。回によって変わる。**具体的な会場名は投稿・リポジトリに書かない**（下記ルール参照） |
| 参加費 | 600円 |
| 持ち物 | 室内用シューズのみ。**バドミントン専用でなくてよい**（外を歩いていない靴ならOK）。**ラケットは無料貸出あり** |
| 1人参加 | 1人で来る人が多い。※**ペアや組み合わせの決め方は未確認。投稿に書かないこと** |
| 参加人数 | 最近は毎回20〜30人（※**投稿には人数を書かない**。初めての人にはプレッシャーになるため） |
| 年齢層 | 20代〜50代 |
| 活動内容 | フリー練習30分 → ダブルスの試合。**1試合5分を3ゲーム回し、そのあと5分休憩** |
| 参加方法 | InstagramのDM → LINEグループに招待。参加できる日だけ参加する方式（毎回の参加表明は事前連絡）。**LINEグループのことは投稿に書かない**（下記ルール参照） |
| 体験 | 1回だけの体験参加も歓迎 |

数字（参加費・人数・時間）を勝手に変えないこと。変更があれば先にこの表を更新してから投稿を作る。

## 2. いまの目標とボトルネック

- **最優先ゴール：新規参加者を増やす**
- 一番のボトルネックは「**そもそも投稿が伸びない**」こと。DMが来ないのではなく、まず見られていない。
  → 「保存されるか」「最初の1秒で刺さるか」を優先して考える。無難で説明的なだけの案は出さない。
- **ターゲットは、バドミントン未経験・初心者で、ラケットも持っていないけどやってみたい人。**
  上級者向けの技術解説や、経験者に向けた煽りは主役にしない。

## 3. 役割分担（Claude が担当する範囲）

- **バドミントンの活動風景の撮影・リール投稿は本人がやる。** Claude はそこには踏み込まない。
- **Claude の担当は、初心者・1人参加が不安な人向けのコンテンツ企画と文章。**
- 成果物の形は **画像カード（このリポジトリの仕組みで 1080×1350 PNG を生成）** ＋ キャプション。
  リール台本を頼まれていないのに書き始めないこと。
- **最優先ネタ：参加当日の流れ（体験レポート風）。** 着いてから帰るまでを時系列で見せて、
  「行ったら何が起きるか分からない」という不安を消すのが狙い。

参考：投稿本数の実績はリール週3・フィード週4・ストーリーも同程度。数を回せる形（1テーマ＝複数枚カード）で提案する。

## 4. 文章ルール（必ず守る）

1. **初心者・1人参加の人を必ず安心させる一文を入れる。** これは全投稿で必須。
   例：「1人だし、下手だし…」で迷ってる人こそ来てほしい／今日初めての人も試合に入れます。
2. **会場名は書かない。** 「和歌山市内の体育館」とぼかす。
   会場は参加連絡をもらってから個別に伝える運用のため。
   リポジトリを公開したため、`drafts/` と `legacy/` に残っていた旧表記もすべて置き換え済み。
   **会場の固有名をこのリポジトリに書き戻さないこと。**
3. **LINEグループのことは投稿に書かない。** DMを送る前から「LINEに招待されます」と分かると、
   連絡先を渡す話に見えてハードルになる。活動日と会場は「個別にお伝えします」「こちらからお知らせします」
   と書き、LINEという言葉自体を出さない。実際の招待はDMのやり取りの中で案内する。
   `weeks/` に残っていた旧表記もすべて置き換え済み。**書き戻さないこと。**
4. **「600円」「ラケット無料貸出」「体育館シューズ」はセットで毎回明記する。**
   ターゲットが未経験・ラケットなしの人なので、ここが一番の不安要素。
   **参加人数（20〜30人など）は投稿に書かない。** 初めての人には「大人数の輪に入る」プレッシャーになる。
   規模を伝えたいときは「1人で来る人が多い」「はじめての人も毎回いる」のように、人数ではなく雰囲気で書く。
   ただし **金額をカードの見出し（大きい文字）にはしない。** 「受付は600円だけ」のような
   見出しは、お金目当てに見えてしまうのでNG。本文か `single` の下部バンドに置く。
5. トーンは丁寧な話し言葉＋絵文字ひとつまみ（🏸 🙌 ⭐️ 🫡 など）。
   煽り口調・体育会系のノリ・出会い目的に見える表現は使わない。
   ⚠️ **決まり文句を全投稿に入れない。** 「1人だし、下手だし…」「大募集です」「こんにちは！和歌山市で〜」は
   一時期9投稿すべてに入っていた。毎回同じ文言は読み手にとって透明になり、飛ばされる。
   安心させる一文（ルール1）は必須だが、**言い方は毎回変えること。**
   また「大募集」は人が集まっていない場所に見えるので使わない。
   キャプションの冒頭1〜2行はフィードで最初に見える枠なので、自己紹介ではなく本題から入る。
6. 締めは「質問がある方や参加希望の方はDMお願いします🤲」系のCTA。
7. 固定ハッシュタグは既存キャプション（`weeks/*/caption.txt`）の末尾ブロックを流用する。

## 5. 投稿を追加する手順

`weeks/<投稿日 YYYY-MM-DD>/` を作り、以下2ファイルを置く。あとは push すれば Actions が
PNGを生成して GitHub Pages に並べる（スマホで開いて保存 → 手動投稿）。

### `cards.json`
カード定義の配列。配列順にレンダリングされ、`01_cover.png` のように連番で出力される。

- `layout: "cover"` … 表紙。フィールド：`headline`, `sub`, `cta`
- `layout: "qa"` … Q&A。フィールド：`icon`, `headline`(Q), `body`(A), `page`（任意 例 `"2/5"`）
- `layout: "single"` … 単票。フィールド：`label`（任意）, `icon`, `headline`, `body`
  - 下部に固定バンド（`遊部（ASOBU）| 和歌山市 | 参加費600円・ラケット無料貸出・参加はDM 🏸`）が自動で入る

**文字数の上限（守らないと帯やイラストに文字がめり込む）**
- `single` の `headline` は **2行まで**（1行9文字が目安）。`body` は **3行まで＝全角42文字程度**
- `cover` の `headline` は **2行まで・1行6文字まで**（7文字だと両脇のキラキラ飾りに文字が触れる）。`sub` は15文字程度まで
- `qa` の `body` は4行（約60文字）まで
- 迷ったら `npm run render` して `dist/` の画像を目視で確認する
- **イラスト・飾りが文字に重なっていないか必ず画像で確認する。** 重なっていたら文字数を減らすか、
  `template.html` 側の飾りの位置を直す（文字を避けるのが正）

共通の書式：
- `headline` は `\n` で改行、`**強調**` でアクセント色になる
- `cover` の `cta` は最初に出てくる数字が自動で強調表示される
- `icon` に使えるのは `person` / `thumb` / `bag` / `yen` / `calcheck` / `pin` / `clock` / `chat` のみ
  （`yen` は「お金目当て」に見えやすいので、料金の話以外では使わない）
  （未定義の名前を書くと `person` にフォールバックする。新しいアイコンが必要なら `template.html` の `ICON` に追加）

### `caption.txt`
Instagramにそのまま貼れる本文。上記「文章ルール」に従う。

### コマンド
```
npm install
npx playwright install chromium   # 初回のみ
npm run render                    # dist/index.html をブラウザで確認
```

## 6. ファイル構成と触ってよい場所

| パス | 役割 | 注意 |
|---|---|---|
| `weeks/<日付>/` | 投稿ごとの `cards.json` ＋ `caption.txt` | 通常の作業はここ |
| `template.html` | カードの見た目。`window.renderCards(cards)` を持つ | デザイン改修時のみ。参加費600円などが `single` のバンドにハードコードされている |
| `render.mjs` | Playwright で 1080×1350 PNG 化し `dist/` にPages用サイトを生成 | 仕組み改修時のみ |
| `.github/workflows/build.yml` | ビルド＆Pagesデプロイ（毎週日曜22:00 JSTにも自動再ビルド） | `main` へのpushでのみ動く |
| `publish.mjs` | Instagramへの投稿（Pages上の画像URLをAPIに渡す） | `MODE=check` で確認、`publish` で投稿 |
| `.github/workflows/publish.yml` | 投稿を手動実行するワークフロー | 自動では動かない。人がボタンを押す |
| `drafts/2026-08-*` | 旧パイプラインの過去ドラフト | 参照のみ。更新しない |
| `legacy/` | 旧パイプライン（Python/Pillow・停止済み） | 触らない |
| `dist/`, `node_modules/` | 生成物 | gitignore済み。コミットしない |

デザイン仕様の出典は作業場の `research/asobu-team/sns/card-design.md`（クリーム地＋二重フレーム／3レイアウト）。
このリポジトリ内にはないので、レイアウトを増やすときは既存の `template.html` の作りに合わせる。

## 7. Instagramへの投稿（半自動）

`publish.mjs` が `weeks/<日付>/` の内容を Instagram に投稿する。Actions から人が手動実行する。

- **自動投稿にはしない。**投稿前に必ず人の目を通すため、`workflow_dispatch` のみ
- 手順は `mode=whoami`（トークン確認）→ `mode=check`（内容確認）→ 問題なければ `mode=publish`
- `check` は次を事前に弾く：画像URLがひらけない／カルーセル10枚超／キャプション2200文字超／
  ハッシュタグ30個超／**キャプションに「◯◯体育館」という固有名が混ざっている**
  （「和歌山市内の体育館」はOK、「体育館シューズ」は持ち物なので対象外）／
  **「9/6」「9月6日」のような過ぎた日付が入っている**（日付のない常設ネタは影響を受けない）
- 日付の入った告知は**その活動日を過ぎたら投稿できない**。Pagesの一覧でも過去の週には「済」が付く
- **画像はPages上の公開URLをAPIに渡す方式**なので、先に build-cards の成功とPagesの公開が必要
- 認証情報は GitHub Secrets の `IG_ACCESS_TOKEN` のみ。`IG_USER_ID` は既定 `me` で不要。コードに書かない
- 予約投稿はAPIにないため、時刻指定が要るならcronで `publish` を叩く形になる

## 7.5 自動投稿（予定日が来たら公開）

`weeks/<日付>/` の**フォルダ名は投稿予定日**。`auto-publish-instagram` が**毎時**動き、
**日本時間21:00〜23:00の回だけ**、その日の日付のフォルダがあれば自動で投稿する。
無ければ何もしない。予定表は別に持たない。

- 日付のない常設ネタも、**投稿したい日をフォルダ名にする**ことで予約投稿になる
- 二重投稿の保険：直近10件の投稿とキャプションを突き合わせ、同じものがあればスキップする
- `publish.mjs` のチェック（会場名・過ぎた日付・画像URL）は自動投稿でも必ず通る。
  引っかかればワークフローが失敗し、投稿されない
- **中身は事前に人が確認する前提。** まとめて作ってPRで確認 → マージ → あとは予定日に自動公開、という運用
- **活動告知は活動日の3日前に出す**（例：9/20の活動 → `weeks/2026-09-17`）。
  日程は本人に聞く。**時間は毎回確認すること**（会場によって取れる枠が変わるため、
  過去と同じとは限らない）
- 投稿時刻は `publish.mjs` の `PUBLISH_FROM_JST` / `PUBLISH_TO_JST`（既定 21〜23）。
  **21時台は暫定値**（夜のスクロール時間帯という一般論で置いた）。
  Instagramのインサイト（フォロワー → 最もアクティブな時間帯）に実データが出るので、
  たまってきたらそちらに合わせて調整する。
  ⚠️ **GitHubのcronは時刻どおりに発火しない。** 1日1回にしていたときは3日連続で3〜4時間遅れ、
  21:17のつもりが深夜1時台に投稿されていた（9/10・9/11・9/12）。
  そのため**毎時起動して、時間帯に入った回だけが投稿する**形にしてある。
  遅れて来た回は何もせず終わり、次の回が時間帯に入れば投稿される。
  時間帯に2回入っても、キャプションの突き合わせで二重投稿は防がれる。
  **1日1回のcronに戻さないこと。**
  なお `auto-publish` を手動実行したときは時間帯の判定を飛ばす（`IGNORE_WINDOW`）ので、
  今すぐ出したいときはActionsから実行すればよい

### 自動投稿が失敗したときの見かた

ワークフローが失敗したら Actions のログを見る。`publish.mjs` はMetaのエラー本文をそのまま出すので、
だいたいそこに答えが書いてある。**失敗しても投稿はされない**ので、慌てて直さなくてよい。

| ログの文言 | 意味と対処 |
|---|---|
| `API access blocked.`（code=200） | **アプリ単位でMetaに遮断されている。** 2026-09-10に発生。原因はMetaの不正ログイン検知で、Instagram側でメールアドレスと電話番号の本人確認をしたら復旧した。まずInstagramアプリに確認要求が来ていないか見る |
| `Session has expired` / `Invalid OAuth access token` | トークンの期限切れ。Metaで取り直して `IG_ACCESS_TOKEN` を更新する |
| `画像URLが◯件ひらけません` | Pagesが未公開かビルド未完了。build-cards の成功を確認する |
| `「◯◯体育館」` / `過ぎた日付` | 原稿の問題。`weeks/` を直す |

⚠️ **GitHub Actionsは実行のたびに違うデータセンターのIPから通信する。**
Metaから見ると毎回知らない場所からのアクセスに見えるため、不審なログインとして
検知される可能性がある。同じことが再発したら、まずInstagramの本人確認を疑う。

ただし **本当に第三者のアクセスだった可能性も毎回検討すること**
（Instagram → 設定 → セキュリティ → ログインアクティビティ）。
本当に不正だった場合は、パスワード変更に加えて**トークンの取り直しが必要**
（アカウントが乗っ取られていればトークンも漏れている）。

## 7.8 企画用のエージェント（秘書＋担当3体）

`.claude/agents/` に常設。CLI・Web・デスクトップアプリのどれからでも同じものが使える。

```
依頼主 ──▶ secretary（秘書・窓口）
                 ├──▶ post-planner          企画
                 ├──▶ target-reader         未経験の読者役として批評
                 └──▶ instagram-strategist  リーチと保存の分析
```

- **依頼は `secretary` に出す。** 担当の名前を覚える必要はない。どこに聞くかは秘書が判断する
- 担当3体は `.claude/agents/staff/` に置いてある。単独で頼みたいときは直接呼んでもよい
- 秘書の報告は形式が決まっている：結論／一致した指摘／割れた点／**事実確認が必要なこと**／次にやること
- 4体とも「CLAUDE.mdにない事実を創作しない」「会場名・人数・金額の見出しは禁止」を前提に持つ
- 担当3体は **読み取り専用**（`Read` / `Glob` / `Grep` のみ）。秘書はこれに担当呼び出しが加わるだけで、
  やはりファイルは書き換えられない。**カードとキャプションを書くのは常にメインの担当**（人の確認を挟むため）
- **1体の意見を鵜呑みにしない。** 複数を並行で走らせ、一致した指摘を優先する。
  実際、2026-09-12の差し替えでは3体が独立に「シューズの説明が全投稿で欠けている」を指摘した
- 役割を足したいときは `.claude/agents/staff/` に同じ形式でファイルを追加し、秘書の担当表にも1行足す

**動いている様子を見る**（Claude Code の標準機能。作り込み不要）
- 実行中のエージェントはプロンプト入力欄の下のパネルにリアルタイムで出る。
  秘書が担当を呼ぶと `(+3)` の入れ子ツリーになる
- **Enter** でそのエージェントのトランスクリプトを開く／**矢印キー**で移動／**x** で停止／**Ctrl+B** で背面へ
- **`/tasks`** で実行中・完了済みの一覧。どのモデルで動いているかも分かる
- 行の色は各定義の `color`（秘書=cyan／企画=green／読者役=yellow／運用=purple）

## 8. 作業の進め方

- 開発ブランチは `claude/badminton-circle-guide-476pv8`。指示がない限り他のブランチに push しない。
- コミットメッセージは既存の慣習に合わせる（例：`docs: add daily Instagram draft` / `feat: ...`）。
- **Pull Request は明示的に頼まれたときだけ作る。**
- 投稿案を作ったら、キャプション本文をそのままチャットにも貼る（スマホからコピペできるように）。
